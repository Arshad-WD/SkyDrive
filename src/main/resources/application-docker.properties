spring.datasource.url=jdbc:postgresql://host.docker.internal:5433/skydrive
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.docker.compose.enabled=false

# jwt
jwt.secret=ThisIsMySuperSecretKeyForskyDriveJwtAuthentication123456789
jwt.expiration=8640000

# minio
minio.url=http://host.docker.internal:9000
minio.access-key=admin
minio.secret-key=password123
minio.bucket-name=skydrive-file

# Redis
spring.data.redis.host=redis
spring.data.redis.port=6379

# ClamAv
clamav.host=clamav
clamav.port=3310

server.port=8080